package com.tuckz.guilds.data

class Attack(var name: String, var description: String) {
    // attack time (how long it takes for the attack to happen in seconds
    // attack speed (how much the characters speed drops)

    val attackTime = 2;
    val attackSpeed = 4;
}